import React from "react";
import { Icon } from "@chakra-ui/react";
import { IoMdCart } from "react-icons/io";

const CartWidget = () => {
  return <Icon as={IoMdCart} fontSize="2xl" />;
};

export default CartWidget;
